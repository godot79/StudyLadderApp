# Resume prompt

Paste the text below into a fresh session to pick this work back up.

---

Read `research/pipeline/NEXT-ROUND-NOTES.md` and `research/pipeline/README.md` first — state as of 2026-08-17 (git log is authoritative over anything below if they conflict; latest commit is `1bdb4e6`, but see "Uncommitted work" below — two merges from this session are NOT yet committed).

**UNCOMMITTED WORK — check this first:** `git status` will show `data/seed/english.json` and `data/seed/maths.json` modified, plus new untracked `research/pipeline/ny-g4-ela-2025/` and `research/pipeline/ny-g4-math-2024/` batch directories, and `research/pipeline/NEXT-ROUND-NOTES.md` modified. These are two fully-reviewed, human-approved, already-merged (via `merge.ts`) batches from this session — NOT half-finished work. Nobody has committed them yet. Ask the user whether to commit before doing anything else that touches git state; do not assume yes, but also do not treat the modified `data/seed/*.json` as unexpected/risky changes to revert — they're intentional.

**Closed out this session (2026-08-17):**
- Ran NY State Regents Grade 4 ELA Spring 2025 (`research/pipeline/ny-g4-ela-2025/`): 17 items, wholly original passages (copyright-safe resolution, same as the earlier UK KS2 batch) written in the style of 3 source passages. One agent run died mid-batch on an account spend limit; resumed cleanly from the completed Step 1 extraction rather than restarting. Found and fixed 2 stylistic softness issues after the batch agent's own review (a chart-integration item whose correct-answer justification overlapped with prose already, and two near-identical "theme" prompts) before merging. Merged: `data/seed/english.json` 271→288. Surfaced two real, still-unfixed pipeline gaps: `verify.ts`/`dedup.ts` have no path for passage-based items (manual verify/dedup required), and `stage.ts` silently strips `passage`/`passageId` fields (its own output file is NOT safe to merge for passage batches — a hand-rebuilt `staging.json` is required each time).
- Ran NY State Regents Grade 4 Mathematics 2024 (`research/pipeline/ny-g4-math-2024/`), the first Math admin ever mined from this archive at any grade: 24 items. Found and fixed a real defect during independent human review that the batch agent's own "re-check every touched item" pass missed: a "which comparison is correct" item had **two true options**, not one (both `1/4 < 1/3` and `3/4 < 4/5` are genuinely true) — this is a distinct failure mode from previously-documented ones (position-rotation corruption, recall-vs-reasoning mislabeling): review must check the truth of ALL options on any comparison/true-statement-identification item, not just confirm the labeled one. Merged: `data/seed/maths.json` 360→384. Also surfaced a recurring `verify.ts` gap: `floor()`/`ceil()`/comparison operators aren't in the `checkExpression` whitelist, same root cause as the earlier units-parsing gap — worked around by hand each time, not fixed generally.

**Current seed sizes (2026-08-17, includes this session's uncommitted merges):** maths 384, english 288, geography 261, science 370, space 219.

**Next natural move:** geography — it's the one core subject not touched by the last two rounds (which targeted english then maths). **Before picking a source, re-check geography's current per-band counts** (`node -e` snippet against `data/seed/geography.json`, same pattern used each round) rather than trusting the 261-total figure above, since band distribution (not just total) drives targeting. The NY State Regents archive likely has an un-mined Grade 4 Social Studies year — check `research/source_catalog.md` and the archive directly; if that's a dead end, fall back to MCAS or Hong Kong TSA/BCA per the source catalog's ranked list. Use `research/pipeline/ROUND-AGENT-PROMPT-TEMPLATE.md` verbatim, `model: sonnet`.

**Standing pipeline gaps that recur and haven't been generally fixed (worked around by hand each round they're hit):**
1. `verify.ts`/`dedup.ts` have no path for passage-based/reading-comprehension items; `stage.ts` strips `passage`/`passageId` — hits any future english/reading-comprehension round.
2. `verify.ts`'s `checkExpression` whitelist doesn't support `floor()`/`ceil()`/comparison operators — hits any future maths round with rounding-logic word problems (rewrite as equivalent whitelisted arithmetic, or use `answerType: "fact"` instead, don't weaken the check).
3. Self-audit's difficulty heuristic is digit/decimal-shaped: false-positives on ALL non-numeric reasoning content (any reading-comprehension subject) and can false-negative genuinely-hard multi-step *interpretive* maths reasoning (floor/ceiling word problems) as "too easy." Treat every flag as "look closer," not a verdict.
4. No automated Step 3 similarity gate (source-closeness check for rewrites) — still not built.
5. History/politics/civics: subject-scope decision (dedicated `history.json`) still approved in principle, still NOT implemented — no seed file, no `SUBJECTS` entry, no `prisma/seed.ts` wiring, ~10-12 existing items still sitting unmigrated inside `geography.json`. Do not implement without proposing it as its own scoped task first.

**Standing rules still apply:** no Prisma schema changes without asking first; no new subject files or schema fields without explicit prior approval; never run `merge.ts` from inside a batch agent — human review (the user, via this session) is always the blocking step between staging and merge.
